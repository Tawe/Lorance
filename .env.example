# =============================================================================
# Frontend Environment Variables
# =============================================================================

# API URL for the backend service
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: Algolia credentials for client-side search (if implemented)
# NEXT_PUBLIC_ALGOLIA_APP_ID=your_algolia_app_id
# NEXT_PUBLIC_ALGOLIA_SEARCH_KEY=your_algolia_search_key

# =============================================================================
# Backend Environment Variables
# =============================================================================

# Required: Algolia credentials
ALGOLIA_APP_ID=your_algolia_app_id
ALGOLIA_ADMIN_KEY=your_algolia_admin_key

# Optional: Index name (defaults to 'lorance_actions')
ALGOLIA_INDEX_NAME=lorance_actions

# Optional: Agent Studio agent ID (enables AI-powered action extraction)
# Create an agent at https://dashboard.algolia.com -> Agent Studio
# Without this, the app uses a fallback regex-based extraction
ALGOLIA_AGENT_ID=your_agent_id

# Optional: Server port (defaults to 3001)
PORT=3001

# Optional: Allowed CORS origins (comma-separated, defaults to localhost:3000)
# In production, set this to your deployed frontend URL
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Node environment (development/production)
NODE_ENV=development
